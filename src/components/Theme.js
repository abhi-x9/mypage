import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#fff",
            light: "skyBlue"
        }, 
        secondary: {
            main: "#15c630", 
            
        },
        otherColor: {
            main: "#999"
        }
    }
});